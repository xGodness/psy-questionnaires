{
  "name": "client_specialist",
  "create": "CREATE TABLE IF NOT EXISTS client_specialist (client_username varchar(32) REFERENCES users (username) ON DELETE CASCADE NOT NULL, specialist_username varchar(32) REFERENCES users (username) ON DELETE CASCADE NOT NULL, PRIMARY KEY (client_username, specialist_username));",
  "insert": "INSERT INTO client_specialist (client_username, specialist_username) VALUES (?, ?);",
  "drop": "DROP TABLE IF EXISTS client_specialist CASCADE;"
}